<?php

use Illuminate\Database\Seeder;
use Illuminate\Support\Facades\DB;
use App\Models\{{modelName}};

class {{modelName}}Seeder extends Seeder
{
    public function run()
    {
        DB::table("{{modelSnakeCaseName}}")->insert([
          
        ]);

        // factory({{modelName}}::class, 10)->create());
    }
}
